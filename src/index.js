import { welcome } from './welcome.js';
import { promptUser } from './userPrompt.js';

// Limpa o console, exibe a mensagem de boas-vindas e chama a função promptUser() para iniciar o programa.
// Clears the console, displays the welcome message, and calls the promptUser() function to start the program.
console.clear();
welcome();
promptUser();
